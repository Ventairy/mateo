# AGENTS.md - MateoHero Extensions

## Naming

All hero extensions must use the `MateoHero*Extension` naming convention.

If an extension is intended to be used only for one type of hero, include the
hero type in the name. For example, use `MateoHeroBoxDragToCloseExtension` for an
extension that only works with `MateoHero.background`.
