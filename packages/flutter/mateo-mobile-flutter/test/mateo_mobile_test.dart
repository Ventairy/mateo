import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'when the mateo_mobile library is loaded, it should compile without error',
    () {
      expect(true, isTrue);
    },
  );
}
