/// The horizontal sizing of a menu.
enum MateoMenuWidth {
  /// Expands to the available width, up to the device-edge limit.
  fill,

  /// Fits content and padding while respecting the available width.
  fit,
}
