/// The horizontal placement of a labeled button's content.
enum MateoButtonAlignment {
  /// Places the content at the left inset.
  left,

  /// Centers the content within the surface.
  center,

  /// Places the content at the right inset.
  right,
}
