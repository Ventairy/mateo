/// The horizontal sizing of a labeled button.
enum MateoButtonWidth {
  /// Expands to the available width within bounded horizontal constraints.
  fill,

  /// Fits the content and padding while respecting parent constraints.
  fit,
}
